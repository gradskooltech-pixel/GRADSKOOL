# Core
setuptools>=70.0.0,<82
Django==5.0.4
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1

# Database
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# CORS
django-cors-headers==4.3.1

# Auth
social-auth-app-django==5.4.0
PyJWT==2.8.0
google-auth==2.29.0
google-auth-oauthlib==1.2.0

# Rate limiting
django-ratelimit==4.1.0

# Email
resend>=1.0.0

# Payments
razorpay==1.4.1

# WhatsApp
requests>=2.31.0,<2.33.0

# HTML sanitization — admin-authored content (blog, FYQ notes, tool pages,
# foundation class notes) is written via Quill.js and rendered raw via
# dangerouslySetInnerHTML on the frontend, with no sanitization anywhere
# before this (2026-08-19). nh3 (Rust bindings, actively maintained —
# bleach's own maintainers now recommend it over bleach itself) strips
# anything outside an explicit tag/attribute allowlist before save.
nh3==0.2.18

# Error tracking
sentry-sdk[django]==1.43.0

# Env
python-decouple==3.8

# Cache
django-redis==5.4.0

# Celery
celery==5.3.6
redis==5.0.3
django-celery-beat==2.6.0

# AI / Transcription
openai==1.30.5

# HTTP
httpx==0.27.0

# Utilities
Pillow>=11.0.0
phonenumbers==8.13.37
gunicorn==22.0.0
whitenoise==6.7.0
