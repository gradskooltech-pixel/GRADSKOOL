# GRADSKOOL — gmat.gradskool.in
# Deploy this config when setting up the GMAT subdomain on Netlify.
# Set NEXT_PUBLIC_EXAM = gmat in Netlify environment variables for this site.

[build]
  command   = "npm run build"
  publish   = ".next"

[build.environment]
  NEXT_PUBLIC_EXAM      = "gmat"
  NEXT_PUBLIC_API_URL   = "https://api.gradskool.in"
  NEXT_PUBLIC_MAIN_URL  = "https://gradskool.in"
  NODE_VERSION          = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# / → /courses/gmat
[[redirects]]
  from   = "/"
  to     = "/courses/gmat"
  status = 302
  force  = false
